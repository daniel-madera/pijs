# PIJS - Personalizovaný inteligentní jazykový sloníček
=======

## Install requirements
sudo apt-get install nodejs npm
// update node via npm
sudo npm cachce clean -f
sudo npm install -g n
sudo n stable
npm install

## Usage
npm test
npm start

